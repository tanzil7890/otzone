import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    image: {
        height: '100%',
        aspectRatio: 1,
        resizeMode: 'cover',
    },

})

export default styles;